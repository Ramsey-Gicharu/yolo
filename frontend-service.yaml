apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 # Update the target port to 80 for nginx
  type: LoadBalancer # Use LoadBalancer to expose externally

